import React from "react";
import "./style.css"

function Title(props) {
    return(
        <nav className= "title">
            <h3>Click on an image to earn points, but don't click it more than once</h3>
        </nav>
    )
}
export default Title;